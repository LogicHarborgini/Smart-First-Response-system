# Upper bounds are deliberate. The previous `langchain>=0.3.0` resolved to
# langchain 1.x — a major version jump that happened to work. Ceilings keep the
# next install from silently landing on an incompatible major.
# Versions below are the ones this project has actually been run against.

# Core AI stack
langchain>=1.3,<2
langchain-aws>=1.6,<2
langchain-core>=1.5,<2
langchain-community>=0.4,<0.5
langchain-ollama>=1.1,<2       # local fallback when AWS Bedrock is unavailable

# Observability
langsmith>=0.10,<1

# API layer
fastapi>=0.141,<1
uvicorn[standard]>=0.52,<1

# Data validation
pydantic>=2.13,<3
pydantic-settings>=2.14,<3

# AWS
boto3>=1.43,<2

# Utilities
python-dotenv>=1.2,<2

# Dev / testing
pytest>=9.1,<10
pytest-asyncio>=1.4,<2
httpx>=0.28,<1                 # for FastAPI test client
